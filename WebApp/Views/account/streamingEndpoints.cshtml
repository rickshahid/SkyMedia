@section head {
    <script type="text/javascript">
        $(function () {
            var columns = [
                {
                    width: 500,
                    name: "name",
                    label: "Streaming Endpoint Name",
                    align: "center"
                },
                {
                    width: 100,
                    label: "Actions",
                    formatter: FormatActions,
                    align: "center"
                }
            ];
            var rows = @Json.Serialize(ViewData["streamingEndpoints"]);
            LoadGrid("streamingEndpoints", columns, rows, "@ViewData["storageCdnUrl"]");
        })
    </script>
}

@section title {
    Streaming Endpoints
}

<table id="streamingEndpoints"></table>