@{
    string gridId = "streamingEndpoints";
}

@section head {
    <script type="text/javascript">
        $(function () {
            _childGridId = null;
            _childPropertyName = null;
            _spacingPatterns = @Json.Serialize(ViewData["spacingPatterns"]);
            _spacingInserts = @Json.Serialize(ViewData["spacingInserts"]);
            _storageCdnUrl = "@ViewData["storageCdnUrl"]";
            var gridId = "@gridId";
            var rows = @Json.Serialize(ViewData[gridId]);
            var columns = GetParentColumns(gridId, "Streaming Endpoint Name");
            LoadGrid(gridId, rows, columns);
        })
    </script>
}

@section title {
    Streaming Endpoints
}

<div class="grid">
    <table id="@gridId"></table>
    <div id="gridPager"></div>
</div>