pipeline {
  agent any
  stages {
    stage('Apply') {
      when {
        expression {
          fileExists('tfFilePath')
        }
      }      
      steps {
        copyArtifacts 'Plan'
        sh '''#!/bin/bash -xe
terraform apply -input=false tfPlan
'''
      }
    }
  }
}
